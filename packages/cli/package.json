{
	"name": "@outbox-reader/cli",
	"version": "1.0.0",
	"description": "Outbox Reader developer tooling — Prisma schema generator and CLI",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"private": true,
	"license": "UNLICENSED",
	"engines": {
		"bun": ">=1.2.0"
	},
	"bin": {
		"outbox-schema": "./bin/outbox-schema.js"
	},
	"scripts": {
		"build": "rm -rf dist && tsc -b",
		"generate": "bun src/cli/generate-schema.ts",
		"lint": "biome check src/",
		"test": "bun test"
	},
	"dependencies": {
		"commander": "^13.1.0"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"@types/node": "^20.3.1"
	}
}
